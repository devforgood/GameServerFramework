CREATE TABLE IF NOT EXISTS player (
    id INT AUTO_INCREMENT NOT NULL,    name VARCHAR(50) NOT NULL,    level INT DEFAULT 1,
    PRIMARY KEY (id)
);

CREATE TABLE IF NOT EXISTS item (
    id INT AUTO_INCREMENT NOT NULL,    name VARCHAR(50) NOT NULL,    level INT DEFAULT 1,
    PRIMARY KEY (id)
);