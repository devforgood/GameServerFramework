// This file is auto-generated. Do not modify directly.

using System;
using UnityEngine;

public static class {{table_name}}Factory
{
    public static {{table_name}} Create(int id)
    {
        {{table_name}} obj = null;
        switch (id)
        {
            {% for item_info in item_infos %}
            case {{item_info[1]}}: obj = new {{item_info[0]}}(); break;
            {% endfor %}
            default: return null;
        }

        Gamedata.{{table_name}} res = null;
		if(!GameManager.Instance.resource.{{table_name}}s.TryGetValue(id, out res))
		{
			Debug.LogError($"{{table_name}} with ID {id} not found in resource {{table_name}}s.");
			return null;
        }
        obj.gamedata = res;

        return obj;
    }
} 