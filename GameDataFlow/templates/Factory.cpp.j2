// This file is auto-generated. Do not modify directly.

#include "Common.h"
#include "{{table_name}}Factory.h"
{% for code_name in code_names %}
#include "{{code_name}}.h"
{% endfor %}


{{table_name}}* {{table_name}}Factory::Create(int32_t id) {
    {{table_name}}* obj = nullptr;
    const gamedata::{{table_name}}* res = ResourceLoader::Instance().Get{{table_name}}s(id);
    if (res == nullptr) {
        return nullptr;
    }

{% if has_code_name_items %}
    const std::string& codeName = res->code_name();
{% for code_name in code_names %}
    {% if loop.first %}if{% else %}else if{% endif %} (codeName == "{{code_name}}") {
        obj = new {{code_name}}();
    }
{% endfor %}
    else {
{% if has_default_items %}
        obj = new {{table_name}}();
{% else %}
        return nullptr;
{% endif %}
    }
{% else %}
    obj = new {{table_name}}();
{% endif %}

    obj->gamedata = res;
    return obj;
} 
